version: '3.9'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: production
      AUTH_REQUIRED: "true"
      MAX_UPLOAD_MB: "20"
    volumes:
      - ./:/app
